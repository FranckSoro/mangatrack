{
  "buildCommand": "cd src && python manage.py collectstatic --noinput",
  "builds": [
    {
      "src": "src/config/wsgi.py",
      "use": "@vercel/python"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "src/config/wsgi.py"
    }
  ]
}